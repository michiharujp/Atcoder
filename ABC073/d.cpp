#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

int main(void) {
    int N,M,R,tmp,A,B,C;
    vector<int> r;
    cin >> N >> M >> R;
    for (int i=0; i<R; i++) {
        cin >> tmp;
        r.push_back(tmp);
    }
    for (int i=0; i<M; i++) {
        cin >> A >> B >> C;
        
    }
    return 0;
}
